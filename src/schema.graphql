# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

"""
A field whose value is a JSON Web Token (JWT): https://jwt.io/introduction.
"""
scalar JWT

input LoginInput {
  blockchain: String!
  signature: String!
  walletAddress: String!
}

type Mutation {
  getNonce(walletAddress: String!): NonceResponse!
  login(input: LoginInput!): Token!
  logout: StatusResponse!
  refreshToken(token: JWT!): Token!
  register(input: RegisterInput!): Token!
  requestEmailOTP(input: RequestOTPInput!): StatusResponse!
}

type NonceResponse {
  isRegistered: Boolean!
  nonce: String!
  walletAddress: String!
}

type Query {
  hello(name: String!): String!
  helloWorld: String!
}

input RegisterInput {
  blockchain: String!
  email: String!
  signature: String!
  walletAddress: String!
}

input RequestOTPInput {
  email: String!
  walletAddress: String!
}

type StatusResponse {
  message: String!
  success: Boolean!
}

type Token {
  """JWT access token"""
  accessToken: JWT!

  """JWT refresh token"""
  refreshToken: JWT!
}